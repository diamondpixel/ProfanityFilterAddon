name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name for release (e.g. v1.0.1)'
        required: true

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x

    - name: Build
      run: dotnet build ProfanityFilterAddon/ProfanityFilterAddon.csproj --configuration Release

    - name: Create HKMP Zip
      run: |
        cd ProfanityFilterAddon/bin/Release/net472
        powershell Compress-Archive -Path ProfanityFilterAddon.dll,ProfanityFilterAddon.xml -DestinationPath ..\..\..\..\ProfanityFilterAddon-HKMP.zip

    - name: Create SSMP Zip
      run: |
        cd ProfanityFilterAddon/bin/Release/netstandard2.1
        powershell Compress-Archive -Path ProfanityFilterAddon.dll,ProfanityFilterAddon.xml -DestinationPath ..\..\..\..\ProfanityFilterAddon-SSMP.zip

    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
      with:
        files: |
          ProfanityFilterAddon-HKMP.zip
          ProfanityFilterAddon-SSMP.zip
        tag_name: ${{ github.event.inputs.tag_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
